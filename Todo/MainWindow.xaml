<Window x:Class="Todo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Todo"
        mc:Ignorable="d"
        Title="MainWindow" Height="480" Width="800">
    <Grid>
        <StackPanel Margin="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBox Width="200" Text="{Binding NewTodoText, UpdateSourceTrigger=PropertyChanged}" KeyDown="MessageBox_KeyDown" FontSize="15"/>
                <Button Content="추가" Command="{Binding AddCommand}" Width="100" Margin="15,0,0,0"/>
            </StackPanel>

            <ListBox ItemsSource="{Binding Items}" Height="300">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Padding="5" BorderThickness="1" BorderBrush="Gray">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsDone}" BorderThickness="3" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Title}" FontSize="20">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Setter Property="TextDecorations" Value="{x:Null}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDone}" Value="True" >
                                                        <Setter Property="Foreground" Value="Gray"/>
                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <Button Content="삭제" Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                                            CommandParameter="{Binding}" Margin="50,0,0,0"/>
                                </StackPanel>
                                <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}"
                                         Height="20" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="저장" Command="{Binding SaveCommand}" FontSize="18" Width="80" Margin="10"/>
                <Button Content="불러오기" Command="{Binding LoadCommand}" FontSize="18" Width="80" Margin="10"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
